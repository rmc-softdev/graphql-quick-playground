import React from "react";
import { render } from "@testing-library/react";
import PokeDex from "../shared/PokeDex";
import { isType } from "graphql";

// import mockCaughtPokemons from "./test-utils/mockdata";

it("should renders an array of todo objects", () => {
  // const mockAddPoke = jest.fn()
  // const mockDeletePoke = jest.fn()
  // const RenderedPokes = render(<PokeDex />);
  // expect(RenderedPokes).toMatchInLineSnapshot();
});
